// tests/services/UserService.test.ts
// import { getUserById } from '../../src/services/auth.service';

describe('Another Feature', () => {
  test('should work as expected', () => {
    // ...
  });

  test.skip('should handle a complex scenario (still thinking)', () => {
    // This specific test will be skipped
  });
});